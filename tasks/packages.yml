- name: Install additional packages.
  apt:
    name: '{{ packages }}'
    state: present
  vars:
    packages:
     - lynis
     - rkhunter
     - logwatch
     - rsyslog
     - snmpd
     - htop
     - ntpdate
     - aide
